apiVersion: v1
kind: Service
metadata:
  name: {{ include "moneyrabbit.backend.fullname" . }}
  labels:
    {{- include "moneyrabbit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moneyrabbit.backend.selectorLabels" . | nindent 4 }}
